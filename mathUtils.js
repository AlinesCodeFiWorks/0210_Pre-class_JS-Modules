//Exercise
//Write two functions: one for adding two numbers (add) and another for multiplying two numbers (multiply).
export function add(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}
